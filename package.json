{
  "name": "image-search-desktop",
  "private": true,
  "version": "1.0.2",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build",
    "prebuild": "npm run check-dist",
    "check-dist": "node -e \"const fs=require('fs');if(!fs.existsSync('dist')){fs.mkdirSync('dist');fs.writeFileSync('dist/.gitkeep','')}\"",
    "postinstall": "node -e \"console.log('Dependencies installed successfully')\""
  },
  "dependencies": {
    "@tauri-apps/api": "^1.5.0",
    "@tensorflow/tfjs": "^4.17.0",
    "@tensorflow-models/mobilenet": "^2.1.0",
    "localforage": "^1.10.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1.5.0",
    "vite": "^5.0.0"
  }
}
